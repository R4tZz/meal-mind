# Supabase JWT Secrets
# Copy this file to .env and replace with your actual values
# For local development, you can use Supabase's demo keys (see documentation)

# PostgREST JWT Secret (min 32 characters)
PGRST_JWT_SECRET=your-jwt-secret-here-minimum-32-characters

# Supabase Anon Key (public key for client-side operations)
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Supabase Service Key (secret key for server-side operations)
SUPABASE_SERVICE_KEY=your-supabase-service-key-here
